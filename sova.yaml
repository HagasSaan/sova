---
behaviour_on_incidents: KillProcess
logfile_path: "/var/log/sova.log"
rules:
  - subject: Path
    condition: MustBeIn
    objects:
      - /bin/cat
      - /bin/ps
      - /bin/env
      - /usr/bin/env
    behaviour_on_violation: KillProcess
