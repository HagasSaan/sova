---
behaviour_on_incidents: KillSystem
logfile_path: "/var/log/sova.log"
rules:
  - subject: path
    condition: MustBeIn
    objects:
      - /bin/cat
      - /bin/ps
    behaviour_on_violation: KillProcess
